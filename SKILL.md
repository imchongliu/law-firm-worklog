---
name: law-firm-worklog
description: 律所工时月报生成（通用版）。从滴答清单拉取指定日期范围内已完成的"业务工作"任务，按用户首次配置的工时模板（列名、固定值、姓名）和项目案号注册表（项目名、案号、关键词）自动归类并生成律所工时系统可导入的 Excel（默认 .xlsx）或 CSV。首次运行会引导用户完成一次性配置（工作项目ID、姓名/律所、列模板、项目案号清单），后续可随时增删项目。当用户提到"工时""工作日志""月度工时""填工时""导出工时""律所工时""X月工时"等时触发，不论是否提及具体律所。
---

# 律所工时月报生成 Skill（通用版）

把"滴答清单已完成任务"转成"律所工时系统 Excel / CSV"的工作流，适用于任何律所、任何使用滴答清单管理任务的律师。

## 总览

本 skill 把工作流拆成两类调用：

- **首次配置**（user-config 目录为空）：引导用户录入律所工时表头、姓名、固定值、滴答工作项目 ID、项目案号注册表。
- **生成工时**（已配置过）：读取配置，拉滴答任务，归类，生成 Excel（默认）或 CSV。

另外随时支持：
- **新增/删减项目** — 用户说"新签了 X 项目，案号 …"或"删掉某项目"时，原地编辑 `user-config/projects.csv`。
- **修改个人信息** — 用户说"换律所了""改姓名"时，编辑 `user-config/profile.md`。

## 起手判断

进入 skill 后按以下决策树走，不跳步：

```
看到 dida MCP 工具？
├─ 否 / "still connecting" 超时 → 告知用户需配置滴答 MCP，停。
└─ 是 → user-config/profile.md 存在且完整？
         ├─ 否 → 流程 A（首次配置）
         └─ 是 → 流程 B（生成工时）
```

**MCP 工具可见但 API 调用报鉴权错误（401/403）**：提示用户"滴答 MCP 授权可能已过期，请在浏览器重新登录滴答账号后重试"，停。不要继续拉数据。

**MCP 工具不可见**的提示文案："这个 skill 需要先在 Claude Code 里装好滴答清单 MCP 才能跑（用来从滴答拉已完成任务）。可以参考 https://github.com/dida365/mcp-server 或滴答官方文档配置 MCP server，配好后回来跟我说一声。"

绝不在此阶段假设默认姓名、默认列名——没有用户认可的配置就不能生成任何输出文件。

---

## 流程 A：首次配置

详细脚本见 `references/setup.md`。要点：

1. 一次性向用户收齐以下信息（一组一组问，不要一次甩一长串）。**能用规则推断的字段不要主动问用户，只在用户 review 时纠正。**

   - **第一组：工时模板**。让用户提供律所工时系统的表头，三种方式任选：
     - 给文件路径（`.csv` / `.xlsx` / `.xls`） → skill 自己读第一行。CSV 用 Read 工具；Excel 用 `openpyxl` 读 sheet 1 第一行，缺包就提示用户 `pip install openpyxl`
     - 粘 Excel/CSV 第一行字符串
     - 逐列列出列名（逗号分隔）

     拿到列名后**按以下启发式自动分类**，把分类结果展示给用户 review，只对 A 类追问"具体填什么"：

     | 列名包含的关键词 | 推断类别 |
     |-----------------|----------|
     | 日期 / date | **B** 滴答任务衍生（任务完成日） |
     | 描述 / 内容 / 任务 / description / task / title / content | **B** 滴答任务衍生（任务标题） |
     | 项目 / matter / project | **C** 项目映射衍生（项目名） |
     | 案号 / case / matter code | **C** 项目映射衍生（案号） |
     | 客户 / client | **C** 项目映射衍生（客户名） |
     | 小时 / hours / time / 时长 / 自报 | **D** 留空待手填 |
     | 其他匹配不到的 | **A** 固定值（追问填什么） |

     **只追问 A 类列的固定值**。其他类别如果用户没纠正，按启发式落盘即可。

   - **第二组：滴答工作项目白名单**。用 `mcp__dida364__list_projects` 列出所有项目，**只问哪几个是"工作"**。其余项目自动不进白名单（不参与 API 过滤），不必让用户去标"个人/学习/家庭"——黑名单不参与任何逻辑，记了也是白记。

   - **第三组：项目案号注册表**。让用户提供当前手上活跃项目的清单。**必问**：项目名 + 案号。**关键词默认 = 项目名本身**（中文项目名通常已经会出现在滴答任务标题里）；只有当项目英文代号/缩写在标题里更常见时（典型如"LDS""PBCL"），才让用户补关键词。**不主动问客户名**——用户想加可以加，不强求。

2. 把收集到的信息写入 `user-config/` 下三个文件：
   - `profile.md` — 工作项目 ID 白名单 / 列分类表（启发式分类结果 + A 类各列的固定值，包括律师姓名）
   - `template.csv` 或 `template.xlsx` — 用户律所工时系统的表头。用户给的是什么格式就存什么格式（给 Excel 存 `.xlsx`，给 CSV 或粘文本存 `.csv` UTF-8 BOM）；如果给的是 Excel 文件路径，除了读表头外，把原文件复制一份到 `user-config/template.xlsx`（保留原格式/列宽/样式，后续生成输出时复用）
   - `projects.csv` — 项目案号注册表，列固定为：`项目名,案号,客户,关键词`（关键词用分号分隔；客户列可留空）

3. 写完后输出"配置已保存到 …，下次直接说'生成 X 月工时'即可"。

**关键约束**：
- 不替用户臆断**列名**和**固定值的具体取值**。但**列的类别**可以靠启发式推断——用户 review 时如果发现误判，纠正即可，成本远低于逐列追问。
- 项目案号绝不虚构。关键词允许默认（项目名本身），不允许凭空编造其他词。

---

## 流程 B：生成工时

### B1. 确认日期范围

向用户确认起止日（如 `2026-05-01` 到 `2026-05-24`）。如果用户只说"5月工时"，按当月 1 日到今天（或当月末，看上下文）处理，并复述给用户确认。

### B1.5 检查输出文件是否已存在

生成前检查默认输出路径（`YYYY-MM-工作日志.xlsx`）：

- **文件不存在** → 继续 B2，正常生成
- **文件已存在** → 提示用户："已存在 `YYYY-MM-工作日志.xlsx`（上次生成于 <文件修改时间>）。是否覆盖？还是生成带时间戳的新文件（如 `YYYY-MM-工作日志-v2.xlsx`）？"

不要静默覆盖——用户可能已经在旧文件里手填了小时数。

### B2. 拉滴答任务（仅工作项目）

调用：
```
mcp__dida364__list_completed_tasks_by_date
```

参数：
```json
{
  "search": {
    "startDate": "<起始日>T00:00:00+08:00",
    "endDate":   "<截止日>T23:59:59+08:00",
    "projectIds": [<读自 profile.md 的工作项目 ID 白名单>]
  }
}
```

**绝不**全量拉取再筛选。`projectIds` 必填，理由是：用户的"个人/学习/还款"项目混在同一滴答账户里，全量拉会把婚礼、还款日、课程都拖进工时，再筛选既慢又容易漏。在 API 层就过滤掉是最稳的做法。

如果用户说"我新建了一个工作项目"或拉回来的数据明显少了，先用 `mcp__dida364__list_projects` 复核 ID 白名单，必要时让用户确认后更新 `profile.md`。

**分页**：如果 `list_completed_tasks_by_date` 的返回结果中包含分页标记（如 `hasMore`、`nextCursor`、`total` 等字段），检查是否已拉全。未拉全则用分页参数补拉后续页，直到全部拿完再进入 B3。不要静默丢弃超出首页的任务。

### B3. 时区转换 + 日期归属

滴答 API 返回 UTC 时间，必须转北京时间（UTC+8）：
- **全天任务**（`isAllDay: true`）：`startDate` 形如 `YYYY-MM-DDT16:00:00+0000`，加 8 小时后落在 **次日**，即 UTC 日期 + 1 才是北京日期。这是个高频踩坑点。
- **非全天任务**：按 UTC+8 直接换算。

按转换后的北京日期归到输出文件"日期"列。

### B4. 按项目案号归类

读取 `user-config/projects.csv`，按以下顺序为每条任务分配项目和案号：

1. **关键词匹配** — 任务标题/描述中包含某项目的任一关键词 → 命中该项目。多个项目同时命中时按以下优先级链裁决：

   1. **完全匹配优先**：关键词完整出现在标题中（如标题含"新能源项目"，关键词也是"新能源项目"）> 仅部分匹配（如关键词是"新能源"，仅命中标题中的子串）
   2. **匹配到的关键词字符数多者优先**（减少单字/短词误命中）
   3. **仍冲突 → 问用户**"以下任务同时命中 [项目A] 和 [项目B] 的关键词，请确认归属"

   例：任务"新能源项目 — 审阅EPC合同"同时命中项目 A（关键词"新能源项目"，5字）和项目 B（关键词"新能源"，3字）→ 项目 A 胜出。
2. **匹配不上** — 不要瞎猜，列出来让用户裁定（"以下 N 条无法自动归类，请告知归属项目"）。

### B5. 生成输出文件

**默认生成 Excel（`.xlsx`）**，用户也可以说"导出 CSV"来要 CSV。

#### 输出路径

文件名默认 `YYYY-MM-工作日志.xlsx`（或 `.csv`），保存到当前工作目录。

#### 输出格式选择

- 用户说"生成 X 月工时"不加后缀 → 输出 `.xlsx`
- 用户说"导出 CSV"或"生成 CSV" → 输出 `.csv`
- `user-config/` 下只有 `template.csv`（没有 `.xlsx`）且用户没指定格式 → 输出 `.csv`（尊重历史配置）

**表头来源不受模板格式限制**：无论 `user-config/` 存的是 `template.xlsx` 还是 `template.csv`，都能从中提取列名数组。所以用户给 Excel 模板、要求导出 CSV（或反过来）完全没问题——列名从模板读，输出格式按用户指令选。

#### Excel 生成（`.xlsx`）

调用 skill 自带的 `scripts/generate_worklog.py`，不要手写 openpyxl：

```bash
python <skill-dir>/scripts/generate_worklog.py \
  --input /tmp/worklog_data.json \
  --output "YYYY-MM-工作日志.xlsx" \
  --template "<user-config/template.xlsx 的路径（如有）>"
```

其中 `/tmp/worklog_data.json` 的结构为：

```json
{
  "headers": ["列1", "列2", ...],
  "rows": [["值1", "值2", ...], ...],
  "column_widths": {"日期": 12, "描述": 50}
}
```

- `headers`：按模板列顺序的列名数组
- `rows`：按通用取值规则填充的数据行（二维数组）
- `column_widths`：可选，指定列宽（key 为列名，value 为宽度数值）；不传则脚本用内置默认值（日期 ~12、描述 ~50、其他 ~15）

脚本会自动处理：加粗灰底表头、细线边框、冻结首行、自动筛选。如果提供了 `--template`，列宽优先从模板继承。依赖 `openpyxl`，首次运行前提示用户 `pip install openpyxl`。

#### CSV 生成（`.csv`）

按 `user-config/template.csv` 的表头顺序写每行。编码 **UTF-8 with BOM**，Excel/WPS 双击不乱码。

#### 通用取值规则（Excel 和 CSV 共用）

按首次配置时的列分类取值：
- **A 类（固定值列）**：直接填 profile.md 里配置好的固定值
- **B 类（滴答任务衍生列）**：
  - 日期列 → B3 算出的北京日期，格式 `YYYY-MM-DD`
  - 描述/标题列 → 滴答任务标题（如有有效的子内容/笔记可附加，但默认只取标题，避免过长）
- **C 类（项目映射衍生列）**：用 B4 归类结果（项目名 / 案号 / 客户名）填入
- **D 类（留空待手填列）**：保持空白。理由：用户最了解每条任务实际花了多少时间或其他主观字段，自动估计反而误导；让用户在 Excel 里手填更准。

### B6. 输出摘要

写完后给用户：
- 文件绝对路径及格式（`.xlsx` / `.csv`）
- 总行数（不含表头）
- 项目分布表（项目 / 案号 / 条数）
- 需用户确认的边界条目清单：未匹配上的任务、关键词命中多个项目的任务

---

## 流程 C：增删项目 / 修改个人信息

用户说"新签 X 项目，案号 …，关键词 …" → 追加一行到 `user-config/projects.csv`，让用户确认后保存。
用户说"删掉 X 项目" → 删行后保存。
用户说"改姓名/换律所/新增/删除一个工作项目 ID" → 修改 `user-config/profile.md` 对应字段。

每次修改后回显"已更新，当前共 N 个项目"。

---

## 重要约束

1. **绝不**全量拉取滴答任务再筛选 —— 必须在 API 层用 `projectIds` 过滤。
2. **绝不**虚构案号 —— 不在 `user-config/projects.csv` 中的项目，先问用户要新案号再写入注册表，再生成输出文件。
3. **绝不**在首次配置完成前生成输出文件 —— 没有用户认可的列模板/姓名/案号，任何 Excel/CSV 都不能直接用。
4. 不强行预估"自报小时"（若列模板中有此列），保留空白让用户手填。

## 维护

- 用户新签项目/收到新案号 → 追加到 `user-config/projects.csv`
- 用户新增滴答工作项目 → 追加到 `user-config/profile.md` 的工作项目 ID 白名单

## 参考文件

- `references/setup.md` — 首次配置详细脚本（一问一答模板、示例）
- `scripts/generate_worklog.py` — Excel 生成脚本（`openpyxl`），已打包，B5 直接调用，不要手写 openpyxl

## 常见问题排查

| 症状 | 可能原因 | 处理 |
|------|---------|------|
| 滴答任务拉不到 / 数量明显偏少 | 工作项目 ID 白名单过时（新建了项目但 profile.md 没更新） | 用 `list_projects` 复核，让用户确认新项目 ID 后更新白名单 |
| 滴答任务拉不到 / 报鉴权错 | MCP token 过期 | 让用户在浏览器重新登录滴答，MCP 会自动刷新 |
| 多条任务无法自动归类 | projects.csv 缺少该项目或关键词覆盖不足 | 展示未匹配任务列表，问用户归属；确认后追加到 projects.csv |
| Excel 打开乱码 | CSV 忘了加 BOM | 确认写入时用了 `utf-8-sig` 编码（或直接改用 .xlsx 输出，无编码问题） |
| openpyxl 报 `ModuleNotFoundError` | 环境没装 | 提示用户 `pip install openpyxl` |
| 生成的任务日期不对（差一天） | 全天任务 UTC 时区没加 8 小时 | 回顾 B3 规则，确认全天任务 `startDate` 的日期部分 +1 |

## 关于"为什么这样设计"

- **配置和工作流分离**：列模板、姓名、案号都是律所/律师强相关的"私有信息"，写在 user-config 里而非 SKILL.md 里，是为了让一个 skill 文件能服务任何律所，配置由用户自己管理。
- **不预设默认值**：律所工时表头差异极大（有的 5 列、有的 8 列、有的有"客户"列没有"项目"列），任何"默认表头"都会让用户拿到的文件导不进系统。宁可第一次多问几个问题，也不要默认值导致后续返工。
- **首跑严格走完配置**：相比"边用边补"，一次性把配置走完，后续每月只需一句"生成 5 月工时"就能完成，体验更稳。
